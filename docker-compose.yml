version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://cache:6379/0
    depends_on:
      - cache
    restart: always

  cache:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: always
